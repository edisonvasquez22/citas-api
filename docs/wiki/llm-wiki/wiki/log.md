# Log — LLM Wiki (append-only)

Registro cronológico de operaciones INGEST/QUERY/LEARN/LINT. No reescribir entradas pasadas; solo agregar.

- 2026-09-17 — INGEST — snapshot inicial de `PRD.md`, `RESTRICCIONES_TECNICAS.md` y `REQUISITOS_NORMALIZACION_3FN.md` a `raw/`; creación de `wiki/dominio.md`, `wiki/arquitectura.md`, `wiki/decisiones.md`, `wiki/riesgos.md`, `wiki/contratos.md` (stub) e `index.md`.
- 2026-09-17 — LEARN — HECHO: la máquina de desarrollo usada para bootstrap no tiene Git/Docker/JDK/Node en PATH; el flujo de compilación/test depende de los contenedores `citas-api-dev`/`citas-web-dev`. Ver [[decisiones]].
- 2026-09-17 — LEARN — DECISIÓN pendiente de confirmación: alcance de HU a aprobar para el incremento S2 (registro + login JWT). Ver [[decisiones]].
- 2026-09-17 — INGEST — investigación acotada delegada a subagente sobre versiones compatibles del stack Spring Boot 3.5.x (parent, MySQL connector, Flyway, JWT, springdoc); resultado fijado en [[decisiones]].
- 2026-09-17 — INGEST — creación del backlog Scrum completo (11 épicas, 24 HU) en `docs/wiki/scrum/`.
- 2026-09-17 — LEARN — DECISIÓN: usuario aprueba HU-006, HU-001 y HU-002 como alcance de S2 (login/JWT completo, sin recortar refresh/logout). Estados actualizados a `En desarrollo`. Comienza implementación del backend hexagonal.
- 2026-09-17 — INGEST — Implementado el backend Spring Boot completo del incremento S2: `pom.xml` (Spring Boot 3.5.16), dominio (`Usuario`, `RolNombre`), puertos y casos de uso de aplicación, adaptadores JWT/BCrypt, adaptadores de persistencia **temporales en memoria** (bloqueados por el diseño 3FN pendiente), `SecurityConfig` + `JwtAuthenticationFilter`, `AuthController` + DTOs + manejo global de errores, y pruebas de dominio/aplicación/integración (`AuthFlowIntegrationTest`). `citas-api/AGENTS.md` generado y `AGENTS.md.template` eliminado. `contratos.md` actualizado con los 4 endpoints de `/api/auth/**`.
- 2026-09-17 — LEARN — HECHO: no se pudo ejecutar `mvn test` ni `mvn spring-boot:run` en el entorno donde se generó este código (sin JDK/Maven/Docker). Toda la evidencia de HU-001/HU-002 quedó marcada `No verificable` en vez de `Cumple`, a la espera de que el usuario ejecute las pruebas en un entorno con el toolchain instalado.
- 2026-09-17 — LEARN — DECISIÓN del usuario: "lo que yo hago es el diseño gráfico, lo demás impleméntelo" — corrige el alcance reservado, que ahora es solo el prototipado visual (Stitch/AI Studio). El diseño de datos pasa a ser responsabilidad del agente.
- 2026-09-17 — INGEST — Diseño 3FN completo del PRD en `citas-api/docs/db-design/MODELO_3FN.md` (ER, dependencias funcionales, justificación), elaborado sin consultar antes `database/reference/db.sql`; comparación posterior en `COMPARACION_REFERENCIA.md`. Migraciones `V1__esquema_inicial.sql` (esquema completo) y `V2__seed_catalogos_fijos.sql` (RF-05/HU-006) creadas.
- 2026-09-17 — INGEST — Reemplazados los adaptadores temporales en memoria por `UsuarioJpaAdapter`/`RefreshTokenJpaAdapter` reales (`@Profile("!test")`). Los dobles en memoria se movieron a `src/test/java/.../testsupport/` (`InMemoryPersistenceTestConfig`) para que las pruebas sigan corriendo sin MySQL. HU-001/HU-002/HU-006 actualizadas con la evidencia correspondiente.
- 2026-09-18 — LEARN — HECHO: usuario instaló JDK 21 (Temurin, vía winget) y Maven 3.9.16 (descarga oficial manual, winget no tiene paquete). `mvn test` ejecutado: BUILD SUCCESS, 15/15 pruebas. Se agregó una prueba nueva (`registro_conDatosIncompletos_devuelve400ConDetalleDeCampos`) para cubrir CA-04 de HU-001, que no tenía prueba dedicada. Evidencia de HU-001/HU-002 actualizada de `No verificable` a `Cumple` para todos los CA de comportamiento; solo la verificación contra MySQL real queda pendiente (falta Docker).
- 2026-09-18 — LEARN — HECHO: reconfirmado en esta sesión que Docker sigue sin estar en PATH (falta completar reinicio/WSL2 pendiente del usuario) y que `mvn test` sigue en BUILD SUCCESS 15/15 sin cambios en `citas-api`.
- 2026-09-18 — LEARN — DECISIÓN: primer export de AI Studio para `citas-web` era un producto no relacionado ("SaludCita", España, Gemini/Express) — descartado. Segundo export corrigió el dominio pero traía una premisa incorrecta (laboratorio quirúrgico para residentes en vez de paciente autorregistrado) y 4 pantallas fuera del alcance aprobado (solo HU-006/001/002 están `En desarrollo`). Usuario eligió integrar solo la pantalla de login, preservando el diseño visual, conectándola al backend real y corrigiendo los textos que contradecían el PRD. Ver detalle completo en [[decisiones]].
- 2026-09-18 — INGEST — Reconciliado `citas-web/front/`: `LoginView` conectado a `POST /api/auth/login` real (con manejo 401/error de red, sin selector de mocks); `SuccessView` con logout real contra `POST /api/auth/logout` y sin campos de perfil inventados; `RegistrationModal`/`PasswordRecoveryModal` convertidos en placeholders honestos; `Header`/`SupportModal`/`App.tsx`/`types.ts` recortados a alcance login-only; dependencias `@google/genai`/`express`/`dotenv`/`lucide-react`/`motion`/`tsx` retiradas de `package.json`; `citas-web/AGENTS.md` generado. Pendiente (requiere confirmación del usuario, bloqueado por guardrail de acciones destructivas): borrar 4 archivos huérfanos fuera de alcance y aplanar `front/` a la raíz de `citas-web`.
- 2026-09-18 — LEARN — HECHO: usuario instaló Node.js 24.19.0 LTS (winget) y GitHub CLI (winget, autenticado como `edisonvasquez22`). `npm install`/`npm run lint`/`npm run build` corridos en `citas-web`: sin errores. Se ejecutó la limpieza pendiente (borrado de 4 archivos huérfanos, `front/` aplanado a la raíz de `citas-web`) con confirmación explícita del usuario. Se crearon repos privados `edisonvasquez22/citas-api` y `edisonvasquez22/citas-web` en GitHub y se subió el trabajo de la sesión (rama `develop` en ambos).
- 2026-09-18 — LEARN — Ejecutado `GOAL_01_GUIADO_SIMPLE.md` (`prompts/goal-loop/`) como verificación formal checkpoint por checkpoint contra el código real de HU-001/HU-002 (no solo evidencia previa), con `mvn test` corrido de nuevo en el momento (15/15, BUILD SUCCESS). Las 6 condiciones de parada se cumplen: registro único, password hasheado, login emite access+refresh, refresh rota la sesión, casos negativos con pruebas dedicadas, `mvn test` pasa. **GOAL_01: PASS**, sin reintentos (funcionalidad ya implementada en el bootstrap de S2; esta corrida fue de verificación, no de construcción). Detalle en [[HU-002-login-y-sesion-jwt]] (vía `docs/wiki/scrum/historias-de-usuario/`).
- 2026-09-21 — LEARN — HECHO: `main` de ambos repos estaba desactualizado (solo el commit inicial) porque todo el trabajo de S2 se había comiteado únicamente en `develop`; el estudiante descargó los repos en otro equipo y no vio el diseño. Se hizo fusión fast-forward `develop → main` en `citas-api` y `citas-web`, y push a GitHub; ambas ramas quedaron sincronizadas.
- 2026-09-21 — LINT — Re-ejecutado el prompt del agente orquestador (`prompts/agents/PROMPT_AGENT_ORQUESTADOR.md`) como auditoría de vigencia, con aprobación explícita del usuario antes de escribir. Se detectó que `wiki/index.md` (resumen vivo) y `wiki/riesgos.md` (riesgo de "ausencia de toolchain") habían quedado desactualizados desde el 2026-09-17 — corregidos. Se detectó una contradicción real: `RESTRICCIONES_TECNICAS.md` exige repos **públicos**, pero se habían creado como privados sin pedírselo al usuario. Con aprobación explícita del usuario, se cambió la visibilidad de `citas-api` y `citas-web` a pública en GitHub. `AGENTS.md` raíz actualizado con el estado real del toolchain (JDK/Maven/Git/Node/GitHub CLI instalados; Docker sigue siendo el único pendiente) y la sección de GitHub.
- 2026-09-21 — LINT — Re-ejecutado el prompt del agente de `citas-api` (`prompts/agents/PROMPT_AGENT_CITAS_API.md`) como auditoría: el código no cambió desde el 17, solo faltaba dejar evidencia fresca. `mvn test` corrido de nuevo: 15/15, BUILD SUCCESS. `citas-api/AGENTS.md` actualizado con esa evidencia.
- 2026-09-21 — LINT — Re-ejecutado el prompt del agente de `citas-web` (`prompts/agents/PROMPT_AGENT_CITAS_WEB.md`) como auditoría: se encontró y corrigió un aviso real de Vite (`__dirname` sin soporte futuro, reemplazado por `import.meta.dirname`). `npm run lint`/`npm run build` verificados en `EXIT 0`. `citas-web/AGENTS.md` actualizado.
- 2026-09-22 — INGEST — Pantalla de registro (HU-001) integrada en `citas-web` (`src/components/RegisterScreen.tsx`), conectada de verdad a `POST /api/auth/register` (maneja 400 con detalle por campo, 409 email/documento duplicado, y error de red), reemplazando el aviso informativo (`RegistrationModal`, ahora huérfano) que abría el login. El export de AI Studio usado como fuente (`citas-web/registro/`, descartado tras extraer lo útil) volvió a traer problemas conocidos: envío simulado con una barra de "presets" de prueba (removida) y una librería de íconos (`lucide-react`) distinta a la ya usada en el resto de la app (homologada a `material-symbols-outlined`); también traía pantallas de agenda/citas y una segunda implementación de login fuera de alcance, descartadas. Verificado con Playwright: dispara el `POST` real, sin errores de consola. `npm run lint`/`npm run build` en `EXIT 0`.
