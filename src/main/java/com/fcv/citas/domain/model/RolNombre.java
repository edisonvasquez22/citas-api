package com.fcv.citas.domain.model;

/**
 * Catálogo fijo de roles (RF-05). Precargado por HU-006; aquí solo se modela
 * el valor que ya usa el dominio de autenticación.
 */
public enum RolNombre {
    USER,
    PROFESSIONAL,
    ADMIN
}
